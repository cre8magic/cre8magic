@using ToSic.Cre8magic.Controls
@inherits MagicControl
<MagicDebug Target="@AllSettings" Title="Current Settings for this Theme">
    <Content>
    @if (AllSettings == null)
    {
        <text>Trying to show debug info for a general settings, but didn't get anything for @nameof(AllSettings)</text>
    }
    </Content>
</MagicDebug>
